mutation UpdateCollaborationTemplate(
  $templateId: UUID!
  $profile: UpdateProfileInput!
) {
  updateTemplate(updateData: { ID: $templateId, profile: $profile }) {
    ...CollaborationTemplateData
  }
}
