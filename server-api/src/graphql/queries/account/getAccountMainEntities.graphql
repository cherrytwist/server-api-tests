query GetAccountMainEntities($accountId: UUID!) {
  lookup {
    account(ID: $accountId) {
      spaces {
        id
      }
      innovationPacks {
        id
      }
      virtualContributors {
        id
      }
    }
  }
}
