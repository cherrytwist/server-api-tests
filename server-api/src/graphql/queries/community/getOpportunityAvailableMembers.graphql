query GetSubsubspaceAvailableMembers(
  $spaceId: UUID_NAMEID!
  $subsubspaceId: UUID_NAMEID!
) {
  space(ID: $spaceId) {
    id
    # TODO: use lookup
    # subsubspace(ID: $subsubspaceId) {
    #   community {
    #     id
    #     ...MembersAndLeadsData
    #   }
    # }
  }
}
